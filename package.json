{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchALL --verbose --coverage"
  },
  "devDependencies": {
    "vite": "^2.3.8"
  },
  "dependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^27.0.5",
    "types": "^0.1.1"
  }
}
